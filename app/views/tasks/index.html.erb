<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box_tag :completed, "1", task.completed, disabled: true %><%= link_to task.title, task_path(task) %> - <%= link_to "remove", destroy_path(destroy) %></li>
  <% end %>
</ul>
<div><%= link_to "Add a new task", new_path %></div>
